getairq < - function(attributes,lat,lon, since, to)
{
  #getairq(attributes,lat,lon, since, to): Function is for getting airquality data related to location and time interval defined by user
  #This function returns air quality data related to user defined variables.
  
}